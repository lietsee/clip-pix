import 'package:flutter/material.dart';

class GridViewModule extends StatelessWidget {
  const GridViewModule({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('GridViewModule placeholder'),
    );
  }
}
